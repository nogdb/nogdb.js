{
  "name": "nogdb.js",
  "version": "0.0.0",
  "description": "nogdb for node.js",
  "dependencies": {
    "mocha": "^5.2.0",
    "nan": "^2.10.0",
    "nan-marshal": "0.0.5"
  },
  "scripts": {
    "test": "rm -rf test.db && mocha && rm -rf test.db"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/nogdb/nogdb.js.git"
  },
  "license": "",
  "gypfile": true
}
